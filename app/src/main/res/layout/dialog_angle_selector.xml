<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/popup_bg"
    android:padding="24dp"
    android:gravity="center_horizontal">

    <!-- Значение угла (чуть меньше, чтобы не мешало 90°) -->
    <TextView
        android:id="@+id/tvAngleValue"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="0°"
        android:textColor="#FF9500"
        android:textSize="36sp"
        android:textStyle="bold"
        android:layout_marginBottom="8dp" />

    <!-- Круг со стрелкой (добавлен отступ сверху) -->
    <com.example.myapplication.AngleCircleView
        android:id="@+id/angleCircle"
        android:layout_width="310dp"
        android:layout_height="310dp"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="20dp" />

    <!-- Кнопки управления -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center">

        <Button
            android:id="@+id/btnToggleMode"
            android:layout_width="0dp"
            android:layout_height="55dp"
            android:layout_weight="1"
            android:text="DEG"
            android:textSize="18sp"
            android:background="@drawable/popup_button_bg"
            app:backgroundTint="@null"
            android:textColor="#FFFFFF"
            android:layout_marginEnd="10dp" />

        <Button
            android:id="@+id/btnConfirm"
            android:layout_width="0dp"
            android:layout_height="55dp"
            android:layout_weight="1"
            android:text="OK"
            android:textSize="18sp"
            android:background="@drawable/popup_button_bg"
            app:backgroundTint="@null"
            android:textColor="#FFFFFF"
            android:textStyle="bold"
            android:layout_marginStart="10dp" />
    </LinearLayout>

</LinearLayout>